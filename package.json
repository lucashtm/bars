{
  "name": "bars",
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "serve": "vite preview",
    "deploy": "vite build && gh-pages -d dist"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^2.4.6",
    "gh-pages": "^6.0.0",
    "svelte": "^4.2.1",
    "vite": "^4.4.9"
  },
  "dependencies": {
    "tinycolor2": "^1.4.2"
  }
}
